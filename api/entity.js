import {resolveEntity,entitySearchTerms} from '../lib/entities.js';
export default async function handler(req,res){const name=String(req.query?.name||'').trim();if(!name)return res.status(400).json({error:'Missing entity name'});const e=resolveEntity(name);return res.status(200).json({id:e.name.toLocaleLowerCase('fa-IR').replace(/[^\p{L}\p{N}]+/gu,'-').replace(/^-|-$/g,''),...e,searchTerms:entitySearchTerms(e)})}
